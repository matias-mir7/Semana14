document.addEventListener('DOMContentLoaded', function() {
    var myCarousel = new bootstrap.Carousel(document.getElementById('textCarousel'), {
        interval: 3000 // cantidad de milisegundos para cambiar el carucel
    });
});